import React from 'react'
import {withRouter} from 'react-router-dom'

function Show(props) {
    return (
        <div>
            
        </div>
    )
}


export default withRouter(Show);
// allows react to add routing props.^